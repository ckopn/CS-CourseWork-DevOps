## Home dev enviropment for PHP Development

```bash
docker-compose up -d
```

* takes about 15 minutes to build all.
* nginx build can fail. re-up in this case, issue in remote server
  response on certification request.
